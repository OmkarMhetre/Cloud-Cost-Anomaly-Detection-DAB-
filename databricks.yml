# This is a Databricks asset bundle definition for cloud_cost_anomaly.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: cloud_cost_anomaly
  uuid: a9d0e361-91bb-4a68-9a43-73881434fe6b

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-5f654bc9-e905.cloud.databricks.com
    variables:
      catalog: finops_catalog_2
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-5f654bc9-e905.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/omkarmhetre800@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/omkarmhetre800@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: finops_catalog
      schema: prod
    permissions:
      - user_name: omkarmhetre800@gmail.com
        level: CAN_MANAGE
